/**
 * @description Default session expiry time in seconds
 * @constant DEFAULT_SESSION_EXPIRY_IN_SECONDS
 * @type {number}
 * @default 7 * 24 * 60 * 60 (7 days)
 * */
export const DEFAULT_SESSION_EXPIRY_IN_SECONDS: number = 7 * 24 * 60 * 60;

/**
 * @description Name of the session token cookie
 * @constant SESSION_TOKEN_COOKIE_NAME
 * @type {string}
 * @default 'session_token'
 * */
export const SESSION_TOKEN_COOKIE_NAME: string = 'session_token';
